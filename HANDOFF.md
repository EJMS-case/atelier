# Atelier — Handoff for the next improvement phase

Refreshed 2026-08-19 on branch `claude/atelier-styling-improvements-uo5ezu`. NEWEST this session:

- **"In Your Looks" wear history (owner request)**: tapping a closet garment now shows, inside EditItemView, when she wore it (dated rows + days-ago + occasion + companion-piece thumbnails), upcoming planner pins (PLANNED pill), and the saved looks featuring it. New `ItemWearHistory.jsx`; App passes `wearData.logs/plans` down — zero AI calls, zero new fetches. The plan/log merge mirrors OutfitHistory's sig-dedupe; if that merge logic ever changes there, change it here too (or extract a shared helper — deliberately not done yet at two call sites). **Watch: does she want tapping a history row to OPEN that look (needs a route to Saved/History with the row focused)? Deferred until she asks.**
- **Open item 10 CLOSED ("+ Create new set" dead-end)**: the fresh set now renders as its own option with an inline Set-name field; name persists on Save via new `onSaveSetMeta` prop → `updateSetMeta`. Verified in Playwright (option + field + live label). Item 5 breadth: SavedLookCard converted to shared `resolveItemIds`.

- **Named pieces beat the weather filter (owner screenshot, same day as #184)**: she named the Terena wool pants verbatim on Hot and still got no pants — the predicted case from the #184 notes, shipped hours later with her explicit confirmation. `nameRescueIds` items now re-union into the pool after step 3/3a (closet-sampler), `checkWeatherCompliance` exempts forceIncludeIds at all three call sites, and the EXPLICIT-REQUEST OVERRIDE block teaches style-around-it (lightest partners, bare shoe). Generous matches still weather-filter; "No …" toggles still always win. **Watch: (a) does her named-piece flow now deliver in any weather; (b) a named+forced HEAVY piece rides looks the validator won't weather-check — if she ever complains a rescued piece was styled wrong FOR the weather, the lever is the style-around-it prompt wording, not a gate.**
- **☀️ Heat palette steering (owner: "much too dark for hot… no hard rules, just smarter")**: her Hot tap produced red-over-forest-green. New HEAT/WARM PALETTE paragraphs in the dynamic weather blocks — light tones lead, ONE deep shade may anchor, dark-on-dark named as a winter story — explicitly taste-not-ban, zero validator changes. **Watch: next few Hot/Warm taps — do looks read seasonally light? If she still gets dark heat looks, tighten the paragraph's wording (it's dynamic-body, edit freely) before considering anything mechanical.**

- **"Theory trousers" force-include bug (owner: 3 taps, zero pants) — root-caused and fixed**: `matchesFreeText`'s brand-anchored fallback counted the brand token as its own "one field hit", so "theory trousers" force-included ALL TEN of her Theory pieces and the model satisfied checkRequestedItems' "≥1 must appear" with a Theory blazer. Fixed three ways in the matcher (non-brand field hit required; brand-only requests still match the whole label; multi-token rule needs ≥2 distinct tokens; plural→singular stem so "pants" lands on "Marcee Pant") + new semantics: the MUST-INCLUDE prompt block now demands EVERY look anchor on a requested piece, and force-included IDs may repeat ACROSS looks (checkNoDuplicates + dedupe salvage + streaming-gate dupe hold all exempt them; within-look dupes still fail; validator floor stays ≥1 hard). **Watch: (a) her next "include my X" tap should feature X in every look — if the model still under-delivers, the lever is checkRequestedItems (could hard-require per-look when a forced item exists for it), (b) rotation now counts a thrice-shown forced piece 3× — harmless (forced items are rotation-exempt) but visible in stats.** NOTE for whoever's next: her "Terena Stretch Virgin Wool Pants" (notes: "Summer, spring, early fall") are weather-filtered out of Hot/Warm on the word "Wool" in the NAME — free text deliberately doesn't bypass weather, so on an August day she cannot style them at all. If she complains, the levers are: a `season_weight` editor in EditItemView (field exists, no UI) letting explicit curation beat the name regex, or a name-rescue weather bypass for literally-named pieces. Ask her first.
- **Evaluator overhaul (owner requests)**: (1) "text cuts off" was the hard `.slice(0,120/160)` caps in evaluateLook.js truncating mid-sentence — now generous rails (280/400) + max_tokens 500→900. (2) **Weather is no longer a rating factor** — new nullable `weather` field in the JSON contract, rendered as a dashed-top italic sidebar row in SilhouetteBuilder ("a little wintery for this heat"); prompt forbids scoring on forecast. (3) **Work looks: bag excluded from score and tips** (she parks it at her desk; one light mention only if it genuinely clashes — scoped to occasion "Work"; Work Dinner still judges the bag, ask her if she wants it there too). (4) Register raised: senior-stylist prompt (The Row/Totême/Khaite calibration), her Settings color pairs in context, tips must be complete concrete fitting-room adjustments, MODEL_STANDARD→MODEL_STRONG. **Watch: ask whether evaluations now read high-end and complete; whether Work Dinner should also ignore the bag; tips are capped at 3 — if she wants more, the cap and the prompt's "one sharp tip beats three reaches" line are the knobs.**
- **First post-#171 `bv`-stamped `ai_errors`**: two `stylist_outfit:recovered` rows on 08-19 (01:41, 05:09 UTC) carry `bv: 13d68c9` with case `looks_string_parsed` — these needed the TOLERANT repair (truncation/garbage), which per #159's design still logs. Not the silent-normalize hole per se, but the strict-parse path is demonstrably still missing some strings on current builds. Low urgency (recovery worked, looks shipped); if rows keep appearing, replay the payloads through coerce-shapes in node per protocol.

- **Layer chips are INCLUDE-mode + Edit routes to the builder (owner requests)**: (1) `GROUP_MODES` in style-filters.js — lower/shoes stay "restrict"; upper (Knits) / outer (Blazers) / legwear (Stockings) are "include": the Only toggle bans NOTHING (it was banning every non-knit top — layering killer), renders an INCLUDE prompt line, keeps the rescue, chip label reads "✚ Include X". Adding a filter GROUP now means FOUR tables: GROUP_DOMAINS, GROUP_NOUN, GROUP_MODES, and (restrict-mode only) GROUP_ONLY_LINES. (2) **LookCard's in-place editor + SwapItemSheet.jsx are REMOVED** — Edit on a results card seeds the manual builder (`builderSeed` in App) with the look's items/tags/layout; the A1 look_edits signal survives via a slot-paired DIFF in the App builder-mount's onSave (swap/remove/add rows + session SWAP LESSONS fold-in). `user_edited` stamping and the mid-stream edited-look reconciliation are gone. **Watch: (a) does "Include Blazers" now reliably produce blazer looks without starving layers; (b) the OutfitHistory/SavedView builder-edit flows do NOT diff-log look_edits (only the results-screen flow does) — extend if her editing shifts there; (c) old saved looks stamped `user_edited` render fine (the badge is just gone).**

- **Signal audit (owner asked for more color-blocking-class defects — collected signals that don't act)**: every user-entered signal traced end-to-end. ONE real gap: `generateTripDayLook` was the last personality-blind AI generator — now its inventory carries f#/pattern/notes digests (120-char cap) and its prompt gets fingerprint (≤800, via new session-memoized `sb.fingerprintTextCached` shared with evaluateLook), color pairings, and silhouette lines, all soft-fail. **Verified clean, don't re-audit without a symptom**: prefs keys, inspiration vibes, loved looks/loves/hearts, About Me paths, look edits, sets tags. **Two deliberate non-issues**: 👎 disliked block fires only on historical rows (thumbs-down UI removed on purpose); `prefs.direction` has no UI editor and rides its hardcoded default ("effortless cool-girl chic…") in every prompt — surface it when the Style Profile surface (roadmap B) is built. Shopping recs keep their static profile by design — adding the fingerprint there is a future lever. **Watch: her next trip-day generation/rebuild should style noticeably more like "her" (formality-matched dinner looks, her pairs, her silhouette rules).**

- **Subcategory chips are two-level (owner report w/ screenshot)**: closet FilterBar + builder picker showed L2 parents and L3 children flat in one row, AND parent selection filtered by literal equality — which matched ~nothing for parents whose rows all carry L3 labels (the dual-labeling item 6 below). New shared `subcatMatches(item, value)` in taxonomy.js (L2-aware via getSubcatL2, per the item's own category) drives both filter predicates; both surfaces now show L2 chips first and expand a selected parent's owned children on a second row (tap selected parent = clear; tap selected child = collapse to parent). `npm run test:taxonomy` (4) added. **Note for item 6 (dual-labeling normalization): the UI no longer depends on which convention a row uses — if normalization ever happens, these chips keep working unchanged.**

- **Style Preferences un-neutered (owner: "is this achieving anything?… restyling a black dress hasn't put it with a navy blazer")**: her Settings color pairs WERE in every prompt but framed as history-derived technique evidence with an explicit "not to restrict palette" — the model was told not to act on them. `formatStylePrefs` now declares them her hand-chosen favorites to PUT TO WORK, teaches that neutrals ground any pair (a neutral hero piece wants a pair color on its partner piece), and the block moved up next to PERSONAL PATTERNS. New 🎯 RESTYLE RANGE directive in the must-include block: re-styles must explore (pair-activation look + tonal/texture/contrast looks), never re-serve a shown color story. All dynamic-body — no cache invalidation. **Watch: ask her after a few black-dress restyles whether the pairs are visibly landing (the navy-blazer-over-black-dress move is the canonical test). If prompt steering still under-delivers, the next lever is deterministic: have the sampler surface pair-partner-colored candidates when a force-included piece is neutral. If she still finds it useless after THIS fix, then remove the feature per her offer.**

- **Winter blazer-under-coat + stockings collage size (owner reports w/ screenshot, 2026-08-13)**: (1) `checkCategoryBalance` is weather-aware — Cool/Cold allows exactly ONE blazer + ONE coat/jacket (shared `isBlazerItem`, item-helpers); everything else still (softly) nags; COOL/COLD dynamic prompt blocks now teach the move; "Only Blazers" domain exempts Coats (overcoats layer OVER blazers — only jacket-weight rivals are banned). Outerwear stacking remains SOFT everywhere — never an error wall. (2) EditorialCollage: hosiery has its own tall singleton slot (leg-shaped box, occupancy-aware, z8 auto-scale / 4 builder-scale) instead of the jewelry-corner accessory chip that made tights render tiny. Collage renders BOTH layers (role cap `layer: 2`; second layer gets a tall side slot at z1 behind the primary — previously the dedupe silently hid it). **Watch: (a) her next Cold taps — does the stylist actually layer blazer+coat, and does the two-layer collage composition read well; (b) do stockings now render at leg size in new looks (saved looks with layout_data keep their old boxes).**

- **Stockings chip (owner request, 2026-08-13)**: `stockings` in FILTER_TYPES rides `isHosieryItem`, single-type "legwear" group — No strips hosiery (safe: hosiery is validator-OPTIONAL, no error wall), Only is a rescue + weather-guarded prompt steer (bans nothing). **Fixed on the way: `describeStyleFilters` had NO "outer" template — "Only Blazers" (shipped same day, #176) would have thrown mid-generation; added outer + legwear templates AND a generic fallback so a future group can never crash it. Lesson: adding a filter GROUP touches three tables — `GROUP_DOMAINS`, `GROUP_NOUN`, `GROUP_ONLY_LINES` — and the last one had no safety net.** test:filters 34→38.
- **Style Me filters (owner requests)**: (1) **Blazers chip** added — new "outer" filter group (`GROUP_DOMAINS.outer` = Outerwear category), "Only Blazers" bans other outerwear only. (2) **Heels chip narrowed to the pump family** — her mules/heeled-thong/dress-flip-flop rows live under Kitten/Heels L3 subcategories, so the chip now requires pump-family subcategory OR pump/stiletto name AND no mule/thong/wedge/sandal-form name (`PUMP_SUBS`/`PUMP_NAME_RE`/`NON_PUMP_RE` in style-filters.js); heeled thong moved to the Sandals chip ("thong" in `SANDAL_RE`). Shared `HEEL_SUBS` (item-helpers) deliberately unchanged — trip-packer heel bans still cover every heel form. test:filters 28→34. **Watch: if she toggles "Only Heels" and misses her mules, the answer is a separate Mules/Slides chip, not re-widening Heels.**

- **Builder picker + evaluator (owner requests, later same session)**: (1) **Comfortwear grouped at the end** — Athleisure/Loungewear pieces in every slot picker now sit behind a collapsed `LOUNGE · ACTIVE · ATHLEISURE (N)` divider at the end of the grid; auto-expands during search or when a comfort-only subcategory chip is active. (2) **SWIM slot chip moved last** in the bar. (3) **New BELT slot** — builder-LOCAL carve-out (`it.category === "Belts"`; ACCESSORY slot now excludes Belts). `slotForItem` deliberately untouched — sampler/validator/rotation still treat Belts as accessories; old saved layouts migrate because restore re-matches items and layout entries key by item id. builderChat's SLOT_CATEGORIES gained `belt`. (4) **Evaluator smarter** (`evaluateLook.js`, rewritten): now sends builder occasion+weather chips as the brief, item notes digests + pattern + `f#` formality (with legend), silhouette lines (About Me, device-local), and the style fingerprint (module-memoized fetch, soft-fail); MODEL_FAST → MODEL_STANDARD; new `works` line in the response + UI. **Watch: ask her whether Evaluate now reads like a stylist who knows her; whether the comfort divider feels right or should also remember its open state; and whether belts placed at the waist need a different default z when layered under outerwear.**

- **Builder picker + evaluator (owner requests, later same session)**: (1) **Comfortwear grouped at the end** — Athleisure/Loungewear pieces in every slot picker now sit behind a collapsed `LOUNGE · ACTIVE · ATHLEISURE (N)` divider at the end of the grid; auto-expands during search or when a comfort-only subcategory chip is active. (2) **SWIM slot chip moved last** in the bar. (3) **New BELT slot** — builder-LOCAL carve-out (`it.category === "Belts"`; ACCESSORY slot now excludes Belts). `slotForItem` deliberately untouched — sampler/validator/rotation still treat Belts as accessories; old saved layouts migrate because restore re-matches items and layout entries key by item id. builderChat's SLOT_CATEGORIES gained `belt`. (4) **Evaluator smarter** (`evaluateLook.js`, rewritten): now sends builder occasion+weather chips as the brief, item notes digests + pattern + `f#` formality (with legend), silhouette lines (About Me, device-local), and the style fingerprint (module-memoized fetch, soft-fail); MODEL_FAST → MODEL_STANDARD; new `works` line in the response + UI. **Watch: ask her whether Evaluate now reads like a stylist who knows her; whether the comfort divider feels right or should also remember its open state; and whether belts placed at the waist need a different default z when layered under outerwear.**

- **Notes policy SHIPPED — open item 11's four interactions are all fixed** (owner directive this session: make notes/descriptions accurate and non-failing). `classifierNotes(item)` in item-helpers.js is the single gate: notes ≤200 chars (curated tags) keep full classifier power; longer notes (pasted product copy — 58 live rows, 69% of all note chars) are excluded from every keyword classifier (validator weather checks, step-3a gate, `filterByWeather`, `isStatementPiece`, `getSleeveType` [which now also reads the NAME], `matchesFreeText` priority 1, occasion rescues/keep-gates/banned keywords, filter chips, trip-packer activity bans). `stylistNotes(notes)` is the prompt-side digest: copy condensed to fabric/silhouette/fit/styling sentences ≤320 chars for `formatInventory` + builder chat (measured on all 58 live rows: −73% chars, digests verified readable); full text untouched for display/search/vision. `npm run test:notes` (21) added to the battery. **Watch: (a) her next Cold-weather taps should stop dropping the silk camis/Eyelet shirts; (b) `CURATED_NOTES_MAX` (200) and `PROMPT_NOTES_MAX` (320) are the tuning knobs; (c) her write-time descriptions feature is still coming — when it lands, a curated "stylist line" field can supersede the digest (see item 11 below, kept for design context).**

- **A5 Silhouette awareness SHIPPED** — and the gap was bigger than the roadmap framed: `aboutMe` was passed into `generateOutfit` but NEVER used (the preamble's "her About Me" rationale hook was dangling). Now `src/features/stylist/silhouette.js` (`npm run test:silhouette`, 47) translates About Me free text into ≤6 reasoned dress-to-flatter lines (petite/tall height parsing, short/long torso rebalance logic, shoulders, hourglass/pear/middle, bust; unmatched text passes through labeled; age+profession → one context line) rendered as the `💃 HER BODY & FIT` dynamic block next to OCCASION MEMORY. Soft-only, no validator changes, preamble byte-identical (no cache invalidation). **Watch: About Me is per-device localStorage — the block only fires once her phone has fields filled. Ask her to fill About Me in Settings, then whether looks feel more flattering. Roadmap next: B (Style Profile surface).**
- **Publishing assessment DONE (owner asked: "can I share an empty Atelier with friends?")** — see the new "Publishing an empty Atelier" section below. Short answer recorded there: not ready as-is (no auth, single shared closet, owner-personalized prompts, no base-schema migration, no onboarding); template-repo path is ~2–4 days of work; a real security finding needs owner action (her Anthropic key is readable via the public anon key + allow-all RLS on `user_settings`).
- **Stale open PRs triaged**: #126 (formality wiring — superseded by the shipped `f#` tokens) and #36 (May-era builder tweaks) should be closed; #151 is half-superseded (#155 shipped its layout fix; the separable-checkbox regression got fixed by another route) but its "+ Create new set" display bug is STILL LIVE — see open item 10 below.
- **Watch-item status**: zero `ai_errors` since 08-10 (none carry `bv` yet); essentially no usage since #171 deployed (1 Style Me look, 1 outfit log), so shirts-in-heat and repeat-feel remain unanswerable until she taps more.
- **Closet notes clamp (PR #173, owner request)**: ItemCard notes clamp to 2 lines with a read-more/less toggle (>60 chars); editing surfaces untouched. Context: her newest descriptions are 400–900-char product copy — see "Owner's long descriptions" in open items (11) BEFORE touching notes-related code; she is building a descriptions feature and will bring it to a session soon.

Previous phase (2026-08-10, PR #171) — ANSWERED both owner survey questions with production data and shipped the fixes, plus roadmap A2 and A4:

- **Survey Q1 (shirts) ANSWERED**: rotation window showed Shirts 1 + T-Shirts 0 of ~22 top slots vs 32/99 closet share. Root cause confirmed in code: NO sampler gate drops shirts in Hot/Warm and the validator has NO long-sleeve rule — HOT's "NO long sleeves" was pure prompt overreach. Fixed prompt-level: "NO **heavy** long sleeves" + a light-woven-shirt steer (worn open over a tank / cuffed / knotted) in HOT and WARM. **Watch: do shirts now appear in heat looks?**
- **Survey Q2 (same items) ANSWERED, three stacked causes**: (1) the 8×-in-24-looks ponte streak was the **sticky free-text request** — "style this item" pre-fills `include my …` and the request box persists across generations, hard-requiring the piece in every re-roll (working as documented, but she may not realize; a clear-after-success or dismissible-chip UX is the NEXT LEVER if complaints persist — deliberately not changed this session); (2) re-roll marathons saturate small buckets and KEEP_FLOOR backfill carried no model-visible signal; (3) identical-name twins rotated as independent ids. Fixed: family-aware rotation (`familyKey`, staleness/backfill/banding), `[JUST SHOWN]` inventory tag on backfilled repeats, stream-time rotation recording, re-entrancy guard. `npm run test:rotation` 14→22.
- **A2 Editorial voice SHIPPED**: rationale is a stylist's card (what the look is doing, ONE gesture, why it suits her, ≤2 sentences) — in the cached preamble (one-time cache invalidation, batched).
- **A4 Occasion memory SHIPPED**: `src/features/stylist/occasionMemory.js` (`npm run test:memory`, 32) digests logs + loves into per-occasion "what she returns to" lines; OCCASION MEMORY prompt block next to HER EDITS; zero per-tap fetches; deliberately NOT fed to the fingerprint generator (it reads the same raw logs already). **Roadmap next: A5 silhouette awareness, then B (Style Profile surface).**
- **`ai_errors` rows now carry `bv`** (build commit SHA, injected in vite.config.js → logError.js): the recurring "stale PWA or real bug?" ambiguity is decidable from the next deploy. Rows WITHOUT `bv` predate PR #171. Context: `looks_string_parsed:recovered` rows kept appearing through 08-08 19:51 UTC despite her force-quits — those remain unattributed; the first post-#171 row with `bv` settles whether #159's silent-normalize has a hole. `look_edits` is STILL empty (she hasn't used the in-place editor).

Previous phase summary (2026-08-08, branch `claude/atelier-casual-hot-warm-z8kx8n`): two more owner-reported fixes shipped — (a) **manual builder reachable again after Style Me results** (declutter regression; she'd been force-quitting — see section C), and (b) **trip round 2**: a swimsuit is its own "Pool" look (never inside a daytime/dinner outfit), on-body leather/suede banned in Hot via `filterByWeather`, and the packer's `scoreForOccasion` now reads curated `formality` (see the CHANGELOG entries + watch items). Also NEW: **the owner answered a priorities survey — read "Owner survey (2026-08-08)" below before picking any next work; it sets the roadmap order (A2 → A4 → A5 → B) and poses two questions she wants answered with data (shirts under-representation; same-items-over-and-over).** Earlier same-day session shipped TWO owner-reported fixes: (1) **Casual + Hot/Warm styles like a stylist** — owner: "not thrilled with the way casual + hot and warm are producing outfits"; diagnosis from her actual morning taps (rotation_state → item resolution: business ponte in casual heat, formality-6 tops over formality-2 lounge pants, 4-piece formula monotony; ZERO validation errors, pure taste problem). Root causes were pipeline contradictions: Casual promptNote advertised knits/boots that step-3a heat gating had removed (the ponte bait), preamble said "NOT athleisure" vs promptNote's athleisure-welcome, WARM block missing the #145 parity pass, ponte in no heat rule, curated `formality` column (171 items) never reaching the prompt (vision_data is 0 rows so `seen:` never fires), no distinctness ask without free text. All fixed as SOFT prompt steering — see the CHANGELOG entry and "Watch after the casual/hot/warm fix" below. (2) **Trip swim packs as complete suits** — owner screenshot: lone swim bottom in every Arizona day card; swim now packs as one-piece-or-pair, 1–2 suits per trip, rebuild-guarded (38 packer tests). Prior session: **Style Me filter chips are wardrobe-aware** (owner request 2026-08-08 — "remove sneakers, I don't have them"): `computeFilterChips` hides unowned types, orders each group by her actual wear frequency, and a Sandals chip was added (14 pairs owned, previously chip-less while the unowned Sneakers chip rendered forever). See the CHANGELOG entry and the Style Me filters note below. Prior session shipped TWO features: (1) the trip-planner capsule overhaul (#163, owner screenshot feedback 2026-08-08) — capsule packing (reuse-first shoes/bags/outerwear/swim with per-slot targets + escape hatch), the "Family Visit" activity, honest Auto-climate behavior (override wins, brief temps used, explained in place), swim finally packable, `npm run test:packer` (24 tests); (2) roadmap **A1 — the stylist learns from her edits**: `look_edits` logging + SWAP LESSONS prompt block + fingerprint fold-in, `npm run test:edits` (13 tests). See the CHANGELOG entries and roadmap items A1/A6 below. Prior sessions: the in-place Style Me look editor (#161 — swap/remove/add a piece on the results screen before saving), then a Style Me page declutter + app-wide copy audit (owner screenshot request 2026-08-08). Prior: PRs #158–#160 (builder crop-hug root cause + aspect-locked resize, string-mode looks first-class, FULL code audit). **NEW: see "Owner-requested roadmap (2026-08-08)" below — she asked for a plan to make the app act like a highly acclaimed stylist and to move features out of Settings; that section is the agreed direction for the next phases.** Previous phase: PRs #146–#157. The #145 deferred-audit list is closed (#146), three owner-reported production bugs were root-caused and fixed (#147, #148), and the closet-image pipeline was overhauled (#149, #150, #152). Read this before starting the next phase. The owner's standing brief: make the app better in any way necessary, go live without preview, keep it efficient/cheap in tokens, and improve **every** aspect.

**Dark mode is REMOVED** (owner request, this phase): no theme toggle, no `THEME_KEY`, no `[data-theme]` CSS. Do not reintroduce. `migrateLocalStorage` strips the orphaned `atelier:theme` key.

## Owner preferences (standing, do not re-ask)

- **Ship it**: feature branch → PR → merge to `main` (auto-deploys via Vercel). She accepts direct merges without preview review.
- **No hard rules that force errors** — a shown look always beats an error. The validator is soft where taste is involved; keep moving in that direction.
- **Priority occasions: Work, Work Dinner, Dinner, Casual.** Occasion + Lounge stay in the Style Me picker; Active/Travel Day/Vacation were removed from Style Me (planner/history still use the full list).
- **No moods** (removed), **no sporty anything** ("Sporty" removed from the vibe vocabulary).
- **Tanks are NOT banned anywhere (2026-08-06, owner request)** — they're layering bases under blazers/knits; per-piece notes steer usage, promptNotes carry the guidance. Don't re-add "Tanks" to any occasion's banned list — and remember bans live in TWO mechanisms: `OCCASION_SLOTS.banned` (styling.js) AND `OCCASION_PREFILTERS.removeSubcategories` (closet-sampler.js). #156 cleared the first; the audit (2026-08-07) found and removed a surviving "Tanks" in the Occasion prefilter. The solo-tank opening got its soft check same-day at her request: `checkTankLayering` (soft `tank_layering` failures for the four dressy occasions, separates looks only, silenced by any Outerwear/Knits layer). Keep it SOFT — it must never produce an error wall.
- **No brand hard rules in shopping recs** (PR #137): she wants suggestions inferred from her actual closet, not a fixed brand short-list. The prescriptive brand lists were removed from both shopping prompts and `STYLE_PROFILE`. Don't reintroduce "use brands X, Y, Z" directives anywhere; the aesthetic-register brand references inside `STYLING_STATIC_PREAMBLE` (taste calibration, not shopping rules) stay.
- **Dark Winter palette, quiet-luxury register** (The Row, Totême, Khaite; easy/feminine: Sézane, Posse…) — see `STYLING_STATIC_PREAMBLE`.
- She wears skirts/minis year-round: hosiery (Accessories > Hosiery, 17 Noosh pairs, every row mapped to a `-v2` display PNG) makes them winter-viable. Never let cold weather exclude skirts.

## Architecture snapshot

- React 18 + Vite, JS only, no TS. Deploys from `main` on Vercel (project `atelier`, team `elycemurillo-8399s-projects`). Anthropic API called **directly from the browser** with the owner's key (Settings → localStorage + `user_settings.api_keys`). Supabase project `ljcwsrfmojbjdveefoqa` (REST, no SDK — `src/lib/supabase.js`).
- Stylist pipeline: `src/lib/ai/stylist.js` `generateOutfit` → `src/utils/closet-sampler.js` → `prompts/styling-system-prompt.js` → `utils/styling-validator.js` (`generateValidatedLooks`: streaming attempt 0 on Opus 4.8 `PRIMARY_MODEL`, retries on Sonnet 5 `FALLBACK_MODEL`, Zod + semantic checks, salvage). Malformed-output repair lives in the pure module `src/utils/coerce-shapes.js` (node-testable — when a new malformation appears, replay the real `ai_errors` payload through it before writing any fix; this protocol caught + fixed case 7 in one pass on 2026-08-02).
- **The AI layer is code-split** (2026-08-02): App dynamic-imports `generateOutfit` / `generateStyleFingerprint` at the call sites, so stylist+zod chunks (~47 kB gzip) load on the first Style Me tap, not cold start. Don't reintroduce a static import of `./lib/ai/stylist.js` (or anything that transitively pulls zod/prompts) into App.jsx — it silently undoes the split. Main chunk is ~318 kB (98.7 gzip); @imgly/onnx were already lazy and only fetch when local bg removal runs.
- Style Me filters: tri-state chips (off → No → Only) per garment type; ALL matcher logic lives in `src/utils/style-filters.js`, shared by sampler + validator (never re-duplicate it). "Only" toggles rescue their type past occasion subcategory bans. **Chips are wardrobe-aware (2026-08-08)**: App renders `computeFilterChips(items, wearData.stats, styleExcludes)` — zero-owned types are hidden (the unowned Sneakers chip is gone; it self-heals back if she adds a pair), most-worn types lead within their structural group, full static list is the cold-start fallback, and an actively-toggled chip stays visible at zero owned. Type roster now includes **Sandals** (subcategory + sandal/slide/flip-flop names, carved out of the Flats matcher like sneakers). Pre-existing nuance, not new: an "Only X" toggle whose type the weather gate empties (e.g. Only Sandals + Cold — step-3a removes all sandals) can starve the shoe pool; if a starved-slot error ever surfaces from an Only toggle, that interaction is where to look. Tests: `npm run test:filters` (28).
- Shopping recs: `generateShoppingRecs` (stylist.js) — both modes on `claude-sonnet-5` with a compact grouped inventory (`summarizeInventory`) and a retry-on-empty wrapper. Coercions live in `coerce-shapes.js` (`coerceRecsShape`).
- AI failures log to the Supabase `ai_errors` table — **check it first** when debugging generation issues. `:schema`/`:no_tool_use` rows record `stop_reason`; `stylist_outfit:recovered` rows carry only a compact `{cases: […]}` list of which repairs fired (full payloads land in `:schema` whenever coercion couldn't finish the job).
- Style fingerprint: generated + stored in `user_settings` (key `style_fingerprint`), auto-refreshes on app load whenever outfit history has grown by 10+ since last generation, feeds the PERSONAL PATTERNS prompt block. Working — do not rebuild.
- Trip weather: `src/lib/geocode.js` (Open-Meteo geocoding, 30 d cache) + `src/lib/weather.js` (16-day forecast: highs/lows, WMO condition labels, precip %; in-memory Map fronts localStorage, 6 h TTL, key v2). Conditions are display-only — packing logic consumes the Hot/Warm/Mild/Cool/Cold buckets. Failure of any kind falls back silently to the seasonal estimate (`~62°F (est)`). Tests: `npm run test:weather` (65).
- Supabase data changes: use the Supabase MCP (`execute_sql` / `apply_migration`). The sandbox's egress blocks direct HTTPS to `*.supabase.co` AND to Open-Meteo — REST scripts won't run from the container; weather tests stub `fetch`.

## Resolved — do NOT re-investigate

- **Work+Hot "no shoes" error wall (2026-08-02, fixed this phase)**: three `stylist_outfit:validation` rows 15:17–15:18 UTC, all "Look 1 has no shoes", Work + Hot. Root cause: Work bans Sandals from the pool while the old HOT prompt block said sandals/open-shoes/light-flats were the only legal footwear and everything else "an automatic failure" — the model omitted shoes; retries re-sent the same contradiction; the shoe-less look STREAMED to screen (the old streaming gate skipped checkShoes) and the no-error-wall rule kept it, so the owner saw incomplete looks, not errors. Fixed four ways: prompt wording aligned to validator reality, retry feedback lists eligible shoe short-IDs, new `salvageByAddingShoes` (logs `stylist_outfit:shoe_salvage`), streaming gate now includes checkShoes + checkWeatherCompliance. Terminal `:validation` payloads now include `pre_salvage` so model-omitted vs salvage-removed is distinguishable. Watch for `:shoe_salvage` rows — a few are fine (that's the safety net working); a spike means the prompt still isn't landing.
- **`looks_string_parsed` on every tap (2026-08-02, addressed this phase)**: every production generation was double-encoding the looks array as a JSON string and paying the coercion repair. `LooksTool`'s description + `looks` property now explicitly demand a raw JSON array. Coercion stays as the safety net. Watch: `stylist_outfit:recovered` rows should drop from ~every-tap to rare; if they don't, the model is ignoring the schema description and the next lever is a system-prompt line.
- **The counteractive-logic sweep is done** — shopping never-invent vs invent, HOT outerwear filter/prompt/validator trichotomy, WARM prompt vs relaxed validator, sampler note-rescue vs checkOccasion, HC2/HC5 preamble overclaims, shoulder rule on "Any". All aligned; don't re-audit these pairs without a new symptom.

- **"Not seeing the real hosiery photos" (2026-08-02)**: `mergeItems` preferred the locally-cached `image` over the fresh Supabase one, so any server-side image replacement was permanently shadowed by every device's localStorage copy. Fixed — server image now wins when present; local stays as fallback for rows with no server image. If a device still shows old art after this deployed, it's the PWA stale-bundle case (force-quit + reopen).

- **Hosiery display images are recolored templates, not raw product photos (2026-08-02, owner request)**: the real Noosh product shots mixed models, poses, and close-ups, which looked chaotic in the grid and collages. The `-v2` PNGs in `scripts/assets/hosiery/` are one clean straight-leg cutout per opacity (sheer/semi/opaque bases), recolored per shade with luminance-preserving colorize — uniform pose, uniform crop. `apply-noosh-photos.mjs` maps rows to the `-v2` files; the un-suffixed originals stay in the repo for reference/reversal. Micro fishnet keeps its real photo. Don't "upgrade" these back to raw product photos without asking her.

- **The 2026-08-01 failure cluster (verified in PR #137)**: owner reported "continued fails". All 14 `stylist_outfit:schema` payloads from 08-01 were replayed through the current `coerce-shapes.js` + Zod in node: 13/14 recover under current code (patterns: stringified looks arrays, `{looks:[…]}` wrappers, trailing `]}`/`</invoke>` garbage, truncation, `<parameter>` fragments, case 7). The 14th (08:29) lost its item IDs in-stream — retry is the correct handling, don't try to "fix" it. Every failure predates the deploy of its covering fix (#131 deployed 18:12 UTC, after the 08:09–14:51 rows; #136 deployed 02:30 UTC 08-02, after the 21:39 case-7 row). Zero `ai_errors` rows since. The `</invoke>`-garbage variant is the "case A2" test. If she still reports fails with NO new `ai_errors` rows, suspect a stale client (PWA service worker `public/sw.js` is cache-first for hashed JS; a long-lived tab/PWA that never re-navigates keeps running old code — force-quit + reopen fixes it), or a failure mode that doesn't log (API key/network) — not the coercion pipeline.
- **Coercion case 7 (2026-08-02)**: the one post-#131 `:schema` row (21:39 UTC) was the model streaming the whole truncated looks array into the `items` slot. Look-shaped values parsed out of `items` are now adopted as `looks` (`items_looks_unwrapped`). The exact payload is a test. (Coerce suite is at 46 tests as of 2026-08-07.)
- **Bundle (2026-08-02)**: the handoff's old framing was stale — @imgly/onnx were already fully lazy. The real cold-start weight was the statically-imported stylist pipeline; now dynamic. See Architecture note before touching App imports.
- **Trip weather (2026-08-02)**: PLAN.md F3 fully closed (real per-destination forecast + conditions + precip + caching). The last F3 leftover — moving outfits between days — shipped this phase (desktop drag + touch day-picker in TripDetailView.jsx).
- **Favorites (PR #132)**: Saved > Favorites merges hearted logs with loved Style Me looks (`look_feedback` rating=1) via `sb.fetchLovedLooks()`, deduped by item set, un-lovable in place. `outfit_logs.is_favorite` confirmed dead and left inert.
- **Piece-hearts now feed the sampler (2026-08-02)**: −0.25 within-band tiebreaker only — deliberately cannot cross a freshness band, so it can't reintroduce repetition. Don't make it bigger without re-reading the anti-repeat rationale in rotation.test.mjs.
- **Stylist schema recovery (PR #131)**: `items` as a stringified item-array fragment → case 6. A fragment holding only a lone id (data genuinely lost) correctly still retries.
- **Shopping + gap analysis** rebuilt (PR #128); prompts de-branded (PR #137). She HAS now used it live — her feedback was the brand short-list complaint, addressed by removing the hard rules so recs are inferred from her closet.
- **`wardrobe_items_backup_20260728` RLS** enabled (service-role-only). Remaining advisor warnings are the app's intentional single-user design.
- **Style fingerprint** exists and self-maintains. **`OutfitBuilder` dead code** removed.
- **"Same pieces over and over" (PR #134)**: look-based 24-deep memory, streamed looks always recorded, LRU floor backfill, freshest-first bucket ordering, cross-device sync via `user_settings.rotation_state`. Tests: `npm run test:rotation` (14).

## 2026-08-07 FULL audit (PR #160) — fixed vs deliberately deferred

Four parallel audit passes (dead code, duplication, counteractive logic, stale docs) ran over the whole repo; every finding was verified against the code before acting. **Fixed in #160:**

- **UTC "today" bug** — SaveLookModal, LooksView, OutfitHistory, and App's onWearAgain used `toISOString().slice(0,10)` (UTC): from ~8pm ET an evening log was stamped with tomorrow's date and the history past/future boundary shifted. All four now use `nyToday()`. (`time.js` remains the ONLY place "what day is it" logic may live.)
- **Tanks still banned on Occasion** via the sampler prefilter (see standing rule above).
- **Streaming gate completed** — now covers every non-negotiable hard check (exclusions, occasion bans, hosiery pairing, shoulder coverage, coord/complete sets, min item count, duplicates). Rationale: a streamed look survives terminal validation failure by design, so the gate is the only barrier between a "No Jeans"-violating look and the screen.
- **Step-3a weather gates extended** to every rule checkWeatherCompliance rejects unconditionally (knits in Hot, heavy fabrics + winter-tagged in Hot/Warm, winter-only pieces in Mild, shorts/swim + summer-tagged in Cool/Cold) — same starvation logic as the #154 boots gate. The gate may only be equal or NARROWER than the validator, never wider.
- **Plan-day plural drift** — pinWornToDate/unpinWornFromDate/handleGenerateForDay/handleAssignSaved now preserve the row's stored `occasions`/`weathers` (via `unionTags` in outfits.js) and `outfit_log_id`; they were silently collapsing builder-authored multi-tags to singletons.
- **Collage z-scale collision** — builder layout z (1–6) and auto-layout z (2–10) mixed raw in `buildFromLayout`, rendering a placed shoe behind an auto-added bag; appended items now remap onto the builder scale.
- **Thumb staleness** — image replacement now also clears the device's local "thumb exists" memory (`forgetThumb`); the server DELETE alone left a permanently stale grid thumb when it failed.
- **Flag holes** — Settings bg-removal batch writes `is_recut`; EditItemView bg removal caps at PHOTO_MAX_DIM.
- **Dead code removed** — `STYLE_PROFILE`, `getSetName`, BulkAdd's `detected` state, LookCard's `heroId` + App's `itemRoles` producer, CalendarView's `isFuture`, `resolveIds`' unused `allItems` param, `saveLookFeedback`'s `mood` param.
- **Consolidations** — `WEATHER_HIGH` (weather.js) + `WEATHER_SHORTS` (taxonomy.js) replace 5 hand-copies; `asArray` replaces 7 inline normalizations in the builder + styleFingerprint's local; `blobToDataUrl` shared via images.js (the SettingsView inline copy could hang forever — no onerror); `PALETTE_STRONG`/`ACCENT_STRONG_HEX` in palette.js; shared `RouteFallback` component; TrimmedImage imports `PHOTO_MAX_DIM`.
- **Docs/comments** — builder + validator + collage + item-helpers headers, README F2/F4/F5/F7, the false "will be rejected" string-mode claim in the DYNAMIC prompt body, apply-noosh comment, mood tooltip.
- **Schema drift closed** — `planned_outfits.outfits` existed only out-of-band; migration 0015 records it (applied as no-op).

**Deliberately DEFERRED (with reasons — pick up when touched next):**

1. **Role-classifier unification.** Three parallel classifiers with real drift: `getGarmentRole` (validator), `slotForItem` (item-helpers), `getRole` (EditorialCollage). Known divergences: Occasionwear non-dress items classify `dress` in two and `top` in the collage; the collage inlines a hand-rolled Sets check instead of `isCompleteSetItem`; three hand-copied Athleisure regex triples (item-helpers has `romper|jumpsuit`/`trouser` additions the others lack). Target: `slotForItem` as the single source + thin adapters. Deferred because collapsing them re-classifies pieces in every rendered collage — needs its own change with visual verification, not an audit side-effect.
2. **Plan-day reconcile consolidation.** The 7 fetch→mutate→buildPlanPayload→savePlan sites still repeat ~200 lines (App ×4, CalendarView ×2, TripDetailView ×2). The DRIFT is fixed (see above); the structural `reconcilePlanDay(date, mutate, opts)` helper in outfits.js is the designed follow-up. The layout_data guard still differs by site (`idx === 0` vs `current.length === 0` vs never) — layout only round-trips for outfit #0, documented limitation.
3. **Hosiery force-include trap** (medium confidence, needs a repro): a free-text request matching ONLY hosiery ("style me with my sheer tights") + model pairing it with trousers → checkHosieryPairing drops the tights → checkRequestedItems fails GLOBALLY → error wall. The #148 fix covers only the literal-name branch. Sketch: salvage must never drop a force-included item (fail that look instead), or checkRequestedItems should tolerate salvage-dropped requests. Don't split the fix from the #148 pair (see 0a).
4. **AI photo-detect duplication**: SettingsView's orphan-recovery loop hand-rolls what `autoDetectItem` (lib/anthropic.js) does with schema validation — but on a different model tier with a different field set. Converging needs a judgement call.
5. **Item-ID → object resolution** appears ~20×. **`resolveItemIds(items, ids)` now EXISTS in item-helpers** (added with the look editor, 2026-08-07) — Map-backed, tolerant of string/number/`{id}` shapes — and LookCard is converted. The remaining ~19 sites (SavedLookCard etc.) are still inline; convert opportunistically when editing those files. Breadth, not depth.
6. **Dead schema candidates — ASK before dropping** (destructive): `moodboards` table (zero code refs), `look_feedback.mood` column (write-only-null, reads removed), `wardrobe_items.primary_color_hex/secondary_color/secondary_color_hex/thumbnail_url` (zero refs; may hold data).
7. **Export-surface trim**: ~20 symbols carry `export` no other module imports (STORAGE_HEADERS, TZ, storage-key consts, etc.) — cosmetic; de-export opportunistically when editing those files.

## Publishing an empty Atelier for friends (assessed 2026-08-11, owner question — awaiting her direction)

The owner asked whether she can share an empty Atelier (prompts included, no closet) with friends. Full code assessment done this session. **Not ready as-is** — the app is deliberately single-user end-to-end:

- **No auth, one shared closet**: no Supabase Auth anywhere; every request uses the hardcoded anon key in `src/lib/supabase.js:6-7`; all RLS policies are `using (true)`; NO table has a `user_id`; `user_settings` is a global singleton keyed by string; the storage bucket is public with no per-user prefix. Two people on one deploy would share/clobber one closet.
- **SECURITY (needs owner action regardless of publishing)**: the repo is PUBLIC and `user_settings.api_keys` holds her **Anthropic API key in plaintext** in a table readable by anyone holding the anon key — which ships in the public repo AND in every deployed JS bundle (anon keys are client-visible by design; rotating it alone fixes nothing). Cheapest real fixes, owner to choose: (a) stop syncing the Anthropic key to Supabase (localStorage-only; loses cross-device key sync — one-line-ish change in App.jsx:1976 + a delete of the row), or (b) an RLS policy hiding the `api_keys` row from anon (breaks the sync feature the same way), or (c) proper auth. Also: README:23 falsely claims the key is "stored locally on your device only."
- **Owner-personalized prompts**: "Elyce" + Dark Winter + NYC + PE-firm context + brand register are hardcoded in `styling-system-prompt.js` (:22,:24,:36,:39,:60), `constants/styling.js` (:10,:22,:24,:38-48, occasion promptNotes "per the user"), `builderChat.js` (~:64), `evaluateLook.js` (EVAL_PROMPT), the Color Advisor end-to-end, and `time.js` (TZ/LAT/LON, "the client lives there"). The learned-signal blocks (PERSONAL PATTERNS / HER EDITS / OCCASION MEMORY / fingerprint / now HER BODY & FIT) are already per-user by construction — the strongest asset for a shareable version.
- **A fresh Supabase project can't be provisioned from this repo**: the base `wardrobe_items`/`user_settings` tables have NO migration file (dashboard-created); migrations start at 0001 assuming they exist. A consolidated `0000_base_schema.sql` is prerequisite to ANY sharing model.
- **No env vars at all** (zero `import.meta.env` usage — Supabase URL/key are source literals; also imported by `seed-hosiery.mjs`/`apply-noosh-photos.mjs`), **no onboarding** (a new user lands on an empty Home and must find the API-key field in Settings), and **small closets can hit error walls** (Work requires layer+bag; Cool/Cold requires hosiery on skirt looks; HC3b needs a top per separates look — a 12-item closet plausibly can't satisfy them; consider softening hard checks below ~40 items for the template).
- **Already fine for sharing**: no-key degradation is graceful everywhere (guards + friendly messages, bulk-add works keyless with local bg removal), PWA manifest/SW generic, no analytics/trackers, deploy is zero-config Vite.

**Recommended path (owner to confirm): template repo** — each friend forks, creates a free Supabase project, runs migrations, sets 2 env vars in Vercel, pastes their own Anthropic key. Full isolation with zero auth code; her live deploy untouched. Work: base-schema migration dump, env-var extraction, prompt genericization behind About Me (+ name/city/palette-or-none fields), first-run onboarding, small-closet softening, gate the Noosh seed scripts, README setup guide. ~2–4 days. Single-deploy multi-tenancy (auth + user_id on 9 tables + RLS rewrite + storage namespacing + ~50 call-site changes) is ~1.5–3 weeks and riskier — defer unless friends actually stick.

## Owner-requested roadmap (2026-08-08) — "best app that acts like a highly acclaimed stylist"

The owner asked two direct questions: how do we make Atelier style like a top-tier human stylist, and can features move out of Settings and get smarter. This is the worked-out plan; pick items up top-down, each as its own PR. Nothing here is started unless marked.

### A. Stylist-grade intelligence (highest leverage first)

1. *(A2 Editorial voice and A4 Occasion memory: SHIPPED 2026-08-10, see the top of this doc. A1 below.)* **Learn from her edits — SHIPPED 2026-08-08** (this branch). Every editor swap/remove/add logs a `look_edits` row (migration 0016, applied to production; fire-and-forget `sb.saveLookEdit`). `summarizeLookEdits` (`features/stylist/lookEdits.js`, pure, `npm run test:edits` 13 tests) collapses repeats into `[Work · Cool] swapped out the black Ankle boot → in the burgundy Kitten heel (×3)` lines feeding (a) the "✂️ HER EDITS" prompt block next to PERSONAL PATTERNS and (b) the style-fingerprint generation (both callers). Same-session edits fold in immediately via App state. **Watch**: once she's edited a few looks, ask whether suggestions feel like they've stopped re-making rejected choices; the aggregation caps (8 prompt lines / 12 fingerprint lines, 120-row fetch) are the tuning knobs. Nothing enforces lessons in the validator — deliberately soft, keep it that way.
2. **Editorial voice — SHIPPED 2026-08-10** (this branch). The `★ RATIONALE WRITING STYLE ★` preamble block now specifies the stylist's card (doing / one gesture / why-her, ≤2 sentences). Watch: do the notes actually read like a stylist now? The block wording is the tuning knob.
3. **Weather should default itself — ALREADY SHIPPED (2026-07-27, `3cb2fe0`; roadmap entry was stale, corrected 2026-08-08).** App.jsx already defaults the Style Me weather chips to today's NYC forecast bucket on load (and again whenever she clears to "Any") via `fetchNycForecast` — chips stay a manual override. Verified in code; do not rebuild. The only unbuilt nuance from the original note is a non-NYC home location stored in `user_settings` — skip unless she actually moves.
4. **Occasion memory / hero pieces — SHIPPED 2026-08-10** (this branch). `summarizeOccasionMemory` (features/stylist/occasionMemory.js, pure, `npm run test:memory` 32) → OCCASION MEMORY prompt block. Tuning knobs: `maxLines`, in-module `MAX_PIECES_PER_LINE`/`HERO_MIN_WEIGHT`/`WINDOW_DAYS` (180 d). Loves count double; twins dedupe by name stem. Watch: once she loves/logs more looks, do occasion looks start centering on her actual heroes?
5. **Silhouette / proportion awareness — SHIPPED 2026-08-11** (PR #172). NOTE the original framing here was wrong: About Me was never "injected raw" — it was silently dropped inside `generateOutfit`. Now translated to explicit guidance via `features/stylist/silhouette.js` (test:silhouette, 47). Tuning knobs: the `DIRECTIVES` map wording and the cue regexes. About Me lives in per-device localStorage (`atelier:about-me:v1`) — no cross-device sync; if she reports "no change," first check whether her phone's About Me is actually filled in.
6. **Trip capsule coherence — SHIPPED 2026-08-08** (this branch). The local packer is capsule-first: `capsuleTargets(dayCount)` bounds distinct shoes/bags/outerwear/swim, reuse gets a bonus, comparable new items are blocked once a slot hits target, with an escape hatch so a day the capsule can't serve (dinner heel on a sneaker trip) still gets the right piece. Bottoms ≤3 wears never back-to-back; tops fresh; statement garments once per trip. Single-day rebuilds seed from the rest of the trip (`priorUse`/`prevDayIds`/`tripDayCount`) so shuffles stay in the capsule. The AI path (`generateTripDayLook`) got matching capsule prompt rules. Tests: `npm run test:packer` (24, jitter-proof margins — keep new assertions above the 0.6 tie-break jitter). Remaining idea if she wants more: color-coherence scoring across the capsule (all pieces mix-and-matchable), not just count-bounding.

### B. Settings decomposition — move taste out, keep plumbing in

Settings today mixes three unrelated things. Split:

- **Stays in Settings (plumbing):** API keys, Photo Backgrounds / trim batches, orphan recovery, data tools. This is maintenance, not styling.
- **Move OUT → a "Style Profile" surface** (new view or a section of Saved/Inspo — owner to pick placement): Style Fingerprint (currently buried; it's the single most stylist-like artifact the app has — surface it as "your stylist's read on you"), Style Preferences (color pairs, modes), About Me. **Make them smarter as they move:** suggest color pairs from her actual loved looks instead of free-text entry; auto-refresh fingerprint stays, but show WHAT changed since last read; About Me → structured chips where possible.
- **Promote out of "More Tools":** Shopping and Color Advisor deserve first-class entry points (Shopping especially — it's rebuilt and brand-agnostic now). Style Intelligence can fold INTO the Style Profile surface (its wear-pattern content and the fingerprint are one story). Visual AI pilot stays tucked away until it graduates.
- Nav is at capacity on the phone (the screenshot shows "Style Me" truncated by the count + saved chips) — any new surface must replace or nest, not append. Consider collapsing the item count + sync chip into one compact indicator before adding anything.

### C. Session hygiene items shipped 2026-08-08 (this branch)

- Style Me page decluttered per screenshot: header "Build manually" button and the empty-state copy + "Open Style Me" button removed; the single "Build a look manually" button remains (the panel already auto-opens via nav, and the collapsed fixed Style Me button covers the closed state). **REGRESSION + FIX (2026-08-08, later session)**: the surviving button lived only in the empty state, so with results on screen the builder was unreachable — she was force-quitting the PWA to get back. A compact "⊞ Build manually" header button now covers the results/loading states; the two render conditions are mutually exclusive, so it's still exactly one affordance per state. If you touch this again, keep that invariant.
- Favorites' dead "Shopping — coming soon" tab removed (re-add only when shopping favorites actually exist; item 8 below updated).
- Copy audit: stale "thumbs-up" wording → ♥ (the control changed in #132 but the copy never followed); wordy empty states in LooksView/OutfitHistory/Favorites trimmed. Remaining copy was checked and is doing work (search placeholders, tri-state filter hint, error guidance).

## Watch after the 2026-08-10 session (PR #171)

- **Shirts in heat**: after a few Hot/Warm taps, re-run the rotation-vs-closet query (join `rotation_state` ids to `wardrobe_items`, group by subcategory) — Shirts/Blouses should climb toward their closet share. If not, the next lever is the HOT/WARM steer wording, not a gate.
- **Repeat feel**: family rotation + `[JUST SHOWN]` tag are live. If she still reports repeats, check whether the repeats are (a) force-included via the sticky request box — then ship the UX lever (clear request after a fresh generation succeeds, or a visible dismissible theme chip in App.jsx); (b) `[JUST SHOWN]`-tagged items the model kept picking — then tighten the tag wording; (c) genuine small-pool saturation on re-roll marathons — mathematically unavoidable at ~17 Work-eligible shoes / ~12 bags with a 24-look window; consider widening the window only with data.
- **First `bv`-stamped `ai_errors` rows**: if `looks_string_parsed:recovered` rows appear WITH a post-#171 `bv`, #159's silent-normalize path genuinely has a hole (strict `JSON.parse` failing on strings the loose parser fixes) — worth a real look. Rows without `bv` are pre-#171 bundles; don't chase them.
- **Editorial voice + occasion memory**: ask her whether the notes read like a stylist's card and whether occasion looks feel like "her" looks. `look_edits` still empty — the A1 watch item (do suggestions stop re-making rejected choices) remains pending her first editor use.

## Watch after the casual/hot/warm fix (2026-08-08)

- **Ask the owner whether Casual + Hot/Warm looks feel better.** The fixes are all soft prompt steering (occasion note, WARM parity, ponte steer, `f#` formality tokens, DISTINCTNESS line) — if ponte still shows up in casual heat looks, the next lever is the formality guidance wording, NOT a hard rule (standing rule: a shown look always beats an error).
- **Formality tokens are live**: inventory lines now carry ` f1`–`f8` on the category segment (171/466 items tagged). If she tags more items via EditItemView the signal improves for free. The preamble changed this session (register line, INVENTORY FORMAT, FORMALITY line, weather blocks) — one-time prompt-cache invalidation, then byte-stable again; don't edit it casually.
- **`look_edits` is still EMPTY in production** (checked 2026-08-08). The A1 edit-learning pipeline shipped 08-08 but she hasn't used the in-place editor since (or a silent fire-and-forget failure — less likely, the write path was verified in code). Once rows appear, ask whether suggestions stopped re-making rejected choices.
- **Stale-PWA signal — superseded 2026-08-10 by the `bv` build stamp** (see the 08-10 watch section): more `looks_string_parsed` rows appeared through 08-08 19:51 UTC despite force-quits, still unattributable; the first post-#171 row with `bv` decides it.
- **Arizona trip rows predate BOTH trip fixes**: she rebuilt after the suit-pairing fix but before trip round 2 (pool-look separation, leather-in-Hot ban, formality scoring) and reported the results as "extremely poor" — bathing suit merged with a skirt look, leather skirts at 105°. All addressed in round 2; she needs ONE more rebuild after that deploy. If the next rebuild still disappoints, the remaining levers are `formalityBand` weights in tripPacker's `scoreForOccasion` and the occasion regexes — get a screenshot first.
- **Trip round 2 watch items**: pool looks render as a second OutfitDraft (label "Pool") on 1–2 days — verify they save/reshuffle correctly on her phone; `filterByWeather` now drops on-body leather/suede in Hot only (Shoes/Bags/Belts/Accessories/Swim exempt) — this function also serves SwapItemSheet/recap, so a Hot-day swap sheet will hide leather garments under its off-weather label behavior.

## Owner survey (2026-08-08) — recorded verbatim intent, steer by this

Asked via in-app survey at her request. Results:

- **Casual misses = ALL FOUR options**: too dressy/corporate, repetitive formula, wrong for weather, pairings she'd never wear. Style Me look quality is her #1 overall priority.
- **Trips**: wants better dinner looks + per-day control (marking days pool/dinner/travel and getting matching looks) as the next trip phase.
- **Roadmap A2/A4/A5/B**: she wants ALL of them; delegated ordering. **Agreed order: (1) A2 Editorial voice, (2) A4 Occasion memory / hero pieces, (3) A5 Silhouette awareness, (4) B Style Profile surface.** Rationale: her top priority is per-look quality; occasion memory attacks the repetition complaint from the "what she returns to" side.
- **Two direct questions — BOTH ANSWERED AND FIXED 2026-08-10** (see the top of this doc for findings and fixes; details kept below for context). Note the closet counts in Q1 below were stale — actual: Blouses 19, Shirts 16, T-Shirts 16 of 99 tops:
  1. **"Why are shirts hardly ever pulled in?"** Hypotheses: Hot's "NO long sleeves" + Warm's "light long sleeves" excludes most blouses/button-downs all summer; freshness-band ordering may have sunk them; model taste. Check: count suggested tops by subcategory (rotation_state + outfit history) vs closet share (Blouses 6, Tops 21, T-Shirts 16, Tanks 12, Bodysuits 8, Polos 3). If it's the sleeve rule, the fix is prompt-level ("light blouse worn open / cuffed is a legitimate hot-weather move"), not a gate change.
  2. **"Why does it keep giving me the same items over and over?"** (repeat of the #134-era complaint, post-rotation.) Strong lead from the 2026-08-08 morning data: she owns near-twin items (Caroline Bag black+brown, Elsa slides ×2 colors, Dalton mules ×2, Shiena flats ×2, Ponte pants ×2) and rotation treats each id independently, so it happily alternates twins — technically fresh, feels identical. Candidate fix: style-family diversity (same name-stem = one family for rotation/anti-repeat). Also: Hot/Warm shrinks shoe+bag pools, making genuine repeats likelier. Verify with data first.

## Known open items (start here)

0y. **2026-08-05 session — three owner reports, all root-caused (PRs #154, #155).** Read before touching the sampler, the schedule path, coercion, or EditorialCollage:
   - **Planner collage drop — TWO independent causes, both needed fixing.** (1) SavedView's `onSchedule` never wrote the incoming `plan.layout_data` (only preserved the existing row's) — manual builds scheduled onto an empty day lost their arrangement; fixed in #154 to match the planner-edit handler. (2) The owner still saw nothing after #154 because **`EditorialCollage` honored `layoutOverride` only on desktop** — on mobile it discarded saved layouts by design ("authored against the desktop landscape canvas"), which was backwards for her data: she builds on her phone, on the builder's portrait 3:4 canvas. The DB proved the rows were fine (nearly all manual plans had `layout_data`). Fixed in #155: manual layouts (discriminator: every entry stamps a `z`; the AI LooksTool schema has none) render on every viewport at 3:4; AI layouts keep desktop-only honoring so saved Style Me looks don't restyle on the phone as a side effect. **Lesson recorded: when a save-path bug and a render-path bug hide the same feature, fixing one makes the other invisible — check the DATA (rows had layouts) before concluding a save fix failed.** Layout still only round-trips for outfit #0 of a day; that's a known, documented limitation, not a bug.
   - **Notes editors enlarged (#155, owner request)**: EditItemView notes input→4-row textarea, BulkAddView→2-row, SaveLookModal 3→5 rows. These are the only three notes-editing surfaces (verified — only one textarea existed app-wide before).
   - **Work+Hot/Warm error wall (12:17 UTC)**: rotation starvation, not wardrobe size. Boots are never suggested in summer → always "fresh" → alone satisfied the shoes `KEEP_FLOOR` once every warm-viable shoe was recently suggested → pool's whole shoe section was boots → model picked one (hard fail) or omitted shoes → salvage had zero candidates. Fixed with a step-3a footwear gate (boots out in Hot/Warm, sandals out in Cool/Cold) mirroring only checkWeatherCompliance's UNCONDITIONAL rules. **General lesson recorded**: the rotation "fresh" test is context-blind — an item that can never validate in the current context can still crowd out usable backfill. The footwear gate fixes the observed case; if a new starved-slot error appears, look here first.
   - **Coercion case 8** (`invalid_layout_stripped`): model-emitted `x/y/w/h` with `w:0,h:0` sank a complete look in Zod. Bad layout numbers are stripped per-item now; the production payload is a coerce test ("case 8").
   - **`looks_string_parsed` — RESOLVED 2026-08-07 by accepting string-mode as first-class.** The prompt-line lever failed too (11/11 `:recovered` rows since 08-05 were `looks_string_parsed`, including post-#155 taps), so the pre-named server-side lever shipped: a cleanly-parsing `looks` string is normalized silently in `coerceLooksShape` — no case recorded, no `ai_errors` row. Strings needing tolerant REPAIR (truncation/garbage) still log. Don't re-add prompt/schema nagging about raw arrays; the model demonstrably ignores it and the string shape is now simply legal. `ai_errors` should go near-silent again — any new `:recovered` row is a real anomaly.
0x. **"Boxes STILL don't hug" (2026-08-06 owner screenshots) — root-caused; it was never the images.** Pixel-audited the two offending items from inside Supabase (temporary `img-audit` edge function on the project, invoked via the SQL `http` extension because the sandbox's egress blocks storage): both images are correctly trimmed transparent cutouts — #149/#150 landed fine. The dead space was box GEOMETRY: (1) the builder's resize handle was free-form (`w`/`h` grown independently), so any manual resize re-letterboxed the piece inside its own box; (2) `layout_data` persisted those boxes and restored layouts are `autoFitted`, so reopening never re-hugged them. Fixed this session: resize is now aspect-locked to the trimmed image, and stale boxes self-heal on load (center-preserving collapse to the contained-image rect — rendered garment pixel-identical). Also fixed a latent flag hole found on the way: EditItemView photo replacement reset `has_bg`/`is_trimmed` to `undefined` (dropped by JSON.stringify → old flags survived in the DB) and never reset `is_recut`, so a padded replacement photo would be skipped by the drip forever; new photos now write `null`/`false`/`false`. **Diagnostic protocol that worked, keep it**: measure the outline's aspect against the image's — `fitBoxToImage` output always matches the image AR exactly, so a mismatched box was by definition not produced by the fit. The `img-audit` function is still deployed (JWT-gated, read-only); delete or reuse at will.
0z. **Closet-image pipeline — overhauled 2026-08-02/03, and the trim backlog is now CLEARED.** Three linked fixes; read all three before touching image code.
   - **#149 — builder boxes hug the garment.** `TrimmedImage` renders the alpha-cropped garment but was reporting the *original* `naturalWidth/Height` through `onLoad`; `fitBoxToImage` snapped each box to the padded photo's aspect ratio while `objectFit:contain` letterboxed the garment inside it. `fitBoxToImage` is the callback's ONLY consumer app-wide — check that before changing what it reports.
   - **#150 — Settings was lying about trim progress.** `is_trimmed` was blanket-set `true` by the bulk import *without the crop ever running*, so Settings claimed all items were trimmed while 430 of 446 still had padding. **`is_recut` is the honest flag** — only written after a real crop, by both the Settings batch and the background drip (they now share it). Don't trust `is_trimmed`. **Status: 462/462 cropped, 0 remaining as of 2026-08-07** — the owner ran the full pass 08-03 and the drip has kept every new upload converged since. The 2026-08-07 audit closed three flag holes: the Settings bg-removal batch now writes `is_recut: true` (it was re-queuing finished work), EditItemView's bg removal caps at PHOTO_MAX_DIM like every other writer, and photo replacement resets all three flags with writable values (#158).
   - **#152 — don't stretch low-res cutouts.** #149 made pieces render up to ~2× larger, which exposed how few pixels some cutouts have ("*some* items are blurry" was the tell — uniform upscaling would soften everything). `fitBoxToImage` now clamps width to the image's own pixel count, floor 40% of the slot default. **`PHOTO_MAX_DIM` (1000) in `utils/images.js` is shared by every write path — bulk add, both Settings passes, the drip — and they MUST stay equal**, or a re-trim silently downscales what an upload just saved. `TrimmedImage`'s `MAX_DIM` matches it; `MAX_CACHE` was dropped to 150 to keep cached pixels flat.
   - **Known limit, don't re-litigate**: `compressImage` never upscales and the original photos were never kept, so re-trimming cannot recover detail — it only removes padding. The raised cap sharpens NEW photos only. Re-trimming *does* sharpen the closet grid, because thumbs are 256px generated from the whole padded frame.
   - **Still open (owner asked for it, agreed order was bulk-trim-then-editor)**: a manual per-piece crop editor in EditItemView, for pieces the automatic crop gets wrong. Not started — worth first asking which pieces actually look wrong now that all 453 are auto-cropped, rather than building it speculatively.
0a. **Free-text force-include: two traps, both fixed (2026-08-02, owner report).** She typed `include my Navy Jumpsuit "Sienna Jumpsuit"` on Work and got no jumpsuit *plus* unexplained navy tights. Same root: `matchesFreeText` is generous by design, so the colour token "navy" force-included a pair of navy tights (hence tights in the collage but absent from the rationale — the model was *told* to use them), while the jumpsuit died to Work's `Jumpsuits` category ban in sampler **step 1**, which runs before force-include in **step 4**. Fixes: a literal name match now clears occasion category+subcategory bans (`namedExplicitly`), and when anything is explicitly named, force-include is restricted to those pieces. Weather filters and active "No …" toggles still apply — deliberately unchanged.
   - **Order matters if you touch this**: dropping a force-included item trips `checkRequestedItems` (hard). Fixing the hosiery rule *without* the force-include fix converts "odd tights" into an error wall. Verified in node before shipping; don't split these two.
0b. **Hosiery pairing is now enforced.** The preamble always said hosiery is legwear for skirts/dresses, but no check backed it, so tights could ship with trousers. `checkHosieryPairing` is hard but **droppable** — salvage removes the tights and the look ships. Skirts/shorts/dresses/jumpsuits pass; trousers/jeans/leggings/ponte fail.
0. **The Work+Warm error wall (2026-08-02 18:22–18:25 UTC) — root-caused and fixed, verify live.** Owner reported "still getting a lot of errors" after #146. It was **our salvage, not the model**: `salvageByDroppingItems` drops every item a hard failure names, and when that item holds a required slot (shoes/bottom) the look loses a piece it can never get back — only shoes had an add-back path. A boot flagged "wrong for Warm" was dropped leaving a shoe-less 2-item look; in one case the wool trousers went too, leaving ONE item. Fixed by a new swap-before-drop salvage (`salvageBySwappingItems`, logs `stylist_outfit:item_swap`) that replaces required-slot offenders in place, like-for-like by garment role. Both production shapes are now regression tests. **Watch:** `:item_swap` rows are the net working (expect some — the model keeps picking the Marrgo Ankle Boot for Warm); a *return* of `:validation` rows naming shoes/lower_half means the swap couldn't find an eligible replacement, which points at the sampled pool, not the salvage.
   - Worth noting for whoever tunes the prompt next: the model repeatedly picked boots and wool for **Warm**. Salvage now rescues those taps, but reducing them at the source is still open — the WARM prompt block never got the same alignment pass the HOT one got in #145.
1. **~~Verify the no-shoes fix live~~ — VERIFIED 2026-08-07**: zero shoe-related `:validation` rows since the #147/#148 deploys across four days of real taps (the 08-05 12:17 cluster was rotation starvation, a different cause, fixed in #154; nothing since). Historical timing notes kept below for context.
   **(original note)** #145 merged at **16:19 UTC on 2026-08-02**. Every `ai_errors` row that exists as of this writing predates that deploy, including a Work+**Warm** no-shoes `:validation` at 16:13 and Work+Hot ones at 15:17–15:18. So none of them test the fix — they are not evidence it failed. The first rows after 16:19 UTC are the real signal. Note the 16:13 row was **Warm**, not Hot: if no-shoes recurs post-deploy, the contradiction isn't Hot-specific and the WARM prompt block needs the same read the HOT one got. Check `ai_errors` for `:shoe_salvage` (fine, rare — that's the net working) vs repeated `:validation` shoes rows (prompt still losing).
2. ~~Watch `looks_string_parsed` frequency~~ — **closed 2026-08-07**: string-mode is first-class now (see 0y); clean parses no longer log at all.
3. **Ask how the de-branded shopping recs feel** (smarter/more personal?) next time she uses Shopping. Note the shopping prompts switched to `SHOPPING_STYLE_PROFILE` this phase (the "never invent items" contradiction is gone) — still zero `shopping_*` rows in `ai_errors`.
4. **Ask the owner how repetition feels.** Both levers live: item-level rotation (#134 + hearts tiebreaker) AND look-combination anti-repeat (`recentCombos` in stylist.js). Rotation sync is now merge-then-write (was last-writer-wins across devices). Watch-signal: still **zero** `no_viable_looks` rows.
5. **Watch `ai_errors`** — protocol unchanged: pull payload, replay through `coerce-shapes.js` in node, then fix. `:validation` rows now carry `pre_salvage`. **Status 2026-08-07: zero rows since 2026-08-06 03:45; the only post-#154 rows were `looks_string_parsed` recoveries, which are now first-class/silent (#159) — any future `:recovered` row is a real repair worth reading**. (Older status: zero rows 08-03 since the #147/#148 deploys), against real activity (5 items added, a Work outfit logged 02:57 UTC). Notably no `:recovered` rows either — those used to fire on essentially every generation, so their absence suggests the `LooksTool` raw-array fix landed. Caveat: a logged outfit could have come from the manual builder, so this is strong but not conclusive proof the no-shoes path is fixed. New kinds to watch: `:item_swap` (#147 swap salvage) and `hosiery` drops (#148).
6b. **`Printed` pants subcategory added (#150, owner request).** `SUBCATEGORY_L3.Pants` now carries `Printed`; the zebra jersey pull-on and striped cotton palazzo are retagged. Deliberately off the otherwise fabric-led axis (Jeans/Satin-Silk/Ponte are materials) — those two share no fabric, and **"Wide Leg" is useless as a discriminator here because nearly every trouser she owns is wide leg**. Banned from Work so they stop reading as tailored trousers, and added to `TROUSER_SUBS` so the garment-type chips still cover them. Accepted inconsistency: her printed *satin* palazzos stay under `Satin/Silk`, so "printed" spans two subcategories.
6. **Legacy dual-labeling** — now quantified against live data (2026-08-02), so the next session doesn't have to re-survey. `wardrobe_items.subcategory` mixes L2 (`TAXONOMY`) and L3 (`SUBCATEGORY_L3`) labels, unevenly by category:
   - **Bottoms is the extreme case**: *zero* rows use the L2 labels "Pants" or "Skirts". All 58 non-Shorts rows are L3 — Trousers 14, Mini 13, Jeans 11, Midi 7, Ponte 5, Maxi 5, Satin/Silk 3. Only "Shorts" (11) is L2. Any matcher testing `subcategory === "Skirts"` matches nothing today.
   - **Shoes is mixed both ways**: L2 Sandals 14, Boots 6, Flats 6, Heels 3 — plus L3 Stiletto 6, Kitten 2 (children of Heels) and Ankle 1 (child of Boots). So 9 of 38 shoe rows are L3.
   - **Accessories carries parent and child simultaneously**: "Scarves & Twillys" 3 *and* "Silk / Twilly" 2; "Jewelry" 1 *and* "Necklaces" 2.
   - **Belts** splits between `""` (11) and `null` (3) — Belts has no subcategories, so this is just an empty-value inconsistency.
   Nothing is broken (matchers accept both), so this is a simplification, not a bug fix. Real normalization = pick one convention, migrate, enforce on write. Do it as its own change with the full battery.
7. **Trip planner**: trips are now deletable (2026-08-08, owner request — "delete so I can regenerate"): "Delete Trip" in the TripDetailView header, confirm → trip row first, then best-effort day-plan clears, then onBack (calendar re-fetches). Verify live that `daily.weathercode` comes back for a real destination; sanity-check move-outfit-between-days on the phone. Trip days now store per-day weather buckets (this phase) — old rows keep the single trip-level bucket until re-saved. **Capsule verification (2026-08-08 later session)**: she DID rebuild the Arizona trip — which surfaced the lone-swim-bottom bug, fixed this session (swim = complete suit, 1–2 placements per trip, `swimPieceKind`/`composeSuit` in tripPacker.js; her swim rows have NO set_id, pairing is name/color-based — if she ever renames swim pieces so "Top"/"Bottom" disappear from names, the classifier defaults to one-piece and pairing degrades; that's the knob to check first). Still to verify: packing-list size drop, climate-pick behavior, and how the reuse feels — `capsuleTargets`, the +1.5/-7 scoring constants are the tuning knobs.
8. **Favorites follow-ups (optional)**: the "coming soon" Shopping sub-tab was REMOVED 2026-08-08 (dead promise, owner-requested copy audit). If shopping favorites ship, add the tab back with real content.
11. **Owner's long descriptions × keyword regexes — FIXED 2026-08-12 (notes policy; kept here for design context when her descriptions feature lands).** Since ~08-05 she's writing/pasting 400–1300-char product-copy notes (58 items >200 chars; closet avg ~160). All four interactions from the 08-11 review are now addressed by the NOTES POLICY (item-helpers.js, `npm run test:notes`):
   - **Weather false positives** (was the sharpest — silk camis/Eyelet shirts/a raffia tote hard-failed Cool/Cold on "pairs with shorts/sandals" copy): every weather classifier now reads `classifierNotes` (curated ≤200-char notes only). Verified against live rows before/after.
   - **Statement false positives** ("metallic hardware"/"lace-up" copy flagging plain trousers into HC8): `isStatementPiece`'s notes scan is gated the same way; pattern fields + curated notes still count.
   - **Free-text amplification** (the 0a "navy tights" trap over 900-char copy): `matchesFreeText` priority 1 reads curated notes only.
   - **Token cost** (her explicit priority): `formatInventory`/builder chat send `stylistNotes` digests — copy condensed to fabric/silhouette/fit/styling sentences, ≤320 chars; measured −73% across all 58 live rows; closet-wide note chars ~74.4k → ~36.9k, and the cap bounds the projected 5× blow-up permanently.
   **For her upcoming write-time feature**: the digest is the interim answer. A curated per-item "stylist line" (short field sent to the prompt; long description kept for display) is still the better end state — if it ships, point `formatInventory` at it and the digest becomes the fallback. The `CURATED_NOTES_MAX`=200 / `PROMPT_NOTES_MAX`=320 thresholds are the tuning knobs; display, closet search, swap-sheet search, and vision enrichment deliberately still read FULL notes.
10. **~~"+ Create new set" still shows no feedback~~ — FIXED 2026-08-19** (see the top of this doc: fresh-set option + inline name field + `onSaveSetMeta`). PR #151 itself was closed 2026-08-19 per this item's triage. Original context kept below:
   **(original note)** From stale PR #151, verified live 2026-08-11: EditItemView's set dropdown derives options only from items already carrying a `set_id` (EditItemView.jsx:238-262), so picking "+ Create new set" mints a UUID that matches no option — the select falls back to "— Not part of a set —" and there's no way to name the new set inline (second garment sees "Unnamed Set (1 piece)"). The other two #151 bugs are fixed by other routes (#155 layouts; separable checkbox restored with `is_separable ?? true` default). Small UX fix: render the fresh id as its own option + an inline set-name field (App already has `updateSetMeta`). PR #151 itself is 9 days stale against an old base — close it and re-implement fresh. PRs #126 and #36 are superseded/ancient — close.
9. **Hosiery images & inventory** (adjusted 2026-08 per owner): the wardrobe has **17** Noosh hosiery items, 15 with real bg-removed product photos. Removed as not owned: opaque brown, opaque skin-tone, and 2 of the 3 seeded micro-fishnet rows — the single owned pair is the renamed "Noosh micro fishnet tights — black" (real photo). PNGs live in `scripts/assets/hosiery/`, applied via `scripts/apply-noosh-photos.mjs` (idempotent); `seed-hosiery.mjs` excludes the not-owned combos. All 17 rows (including skin-tone and brown semi-opaque) map to shipped `-v2` PNGs in `apply-noosh-photos.mjs`.

### Deferred-audit list — CLOSED this phase (PR #146), do not re-open

All seven items below were the #145 audit's deferred backlog. Six are done; one was
deliberately left alone. Don't re-audit these without a new symptom.

- **Weather-band predicates** → `weatherMatches(w, ...buckets)` in `taxonomy.js`, built on the formerly-unused `WEATHER_BUCKETS`. Tests each named bucket independently (unlike first-match-wins `weatherBucketOf`) so combined labels still match several buckets, as the old inline regexes did. All ~6 sites converted.
- **Model-ID constants** → `src/constants/models.js` (`MODEL_TOP`/`MODEL_STRONG`/`MODEL_STANDARD`/`MODEL_FAST`), 17 magic strings across 11 files retired. No model assignments changed.
- **`resolveLookItems` + statement detector** → both done. Resolution helpers live in `styling-validator.js`; `isStatementPiece`/`STATEMENT_PATTERNS` moved to `item-helpers.js` and are shared with tripPacker, so the KEEP IN SYNC copies are gone. The packer's genuine differences are now explicit: a `fringeCounts` option and the hosiery exemption. It also picked up `featherwork` (the validator already had it) — an intentional convergence, not a regression.
- **Home triple-fetch** → App owns one `refreshWearData`; concurrent callers share the in-flight promise. HomeView and LookBackCard filter their own date windows from the shared rows. A fourth redundant `fetchAllPlans` in the fingerprint path is gone. **SilhouetteBuilder** is now `lazy()` in LooksView/OutfitHistory — it was already its own chunk, but the Saved chunk imported it statically, so Saved-tab open eagerly fetched ~28.6 kB (9.5 kB gzip).
- **sw.js cache** → stamped per build. **The stamping is a build STEP** (`vite build && node scripts/stamp-sw.mjs`), deliberately NOT a Vite plugin: Vite copies `publicDir` into `dist` *after* `closeBundle`, so a plugin's stamp gets overwritten by the pristine `public/sw.js`. Two traps are worth remembering if you touch this — the placeholder token must not appear in sw.js's own prose (the stamper would rewrite that instead; it uses `replaceAll` now), and a missing placeholder **throws and fails the build** on purpose, because a silent no-op here regresses to one constant cache name and nobody would notice for months.
- **PALETTE** → `src/constants/palette.js` + a new `--color-accent-strong: #6D1A2E` token. Calendar/TripDetail keep a literal hex because they build alpha variants by string concatenation (`${PALETTE.accent}0A`), which a `var()` can't do. Zero rendered change across all six views.
- **supabase.js PGRST204 retry loop** (quadruplicated across upsert/saveOutfitLog/updateOutfitLog/savePlan) — **still open, still deliberately deferred.** It's the one item genuinely behaviour-risky to merge, and it has no symptom attached. Leave it unless something breaks.

## Efficiency / token-cost targets (owner explicitly wants this)

- **Contact sheets (reworked this phase)**: geometry now sits under the API's ~1.15 MP downscale cap (90px thumbs, 120 items/sheet, 900×1224) — typical 160-item tap costs ~1,959 vision tokens, down ~36% from ~3,067, attempt 0 only. Token math is in the contact-sheet.js header; don't enlarge thumbs past the cap or the extra pixels are billed then thrown away. Remaining levers if she wants more: fewer/conditional sheets on re-rolls (the image cache already makes re-rolls fast, but tokens are per-call), or skipping sheets for small pools.
- **Prompt caching**: static preamble is cached (`cache_control: ephemeral`); keep it byte-stable — any edit invalidates the cache for every user tap.
- **Bundle**: main chunk ~326 kB (101.5 gzip) as of the look editor (SwapItemSheet + LookCard edit mode ride the main chunk — LookCard is statically imported by App); before that ~318 kB (99.1 gzip). The earlier win was deferring the builder chunk (~28.6 kB / 9.5 gzip) off the Saved-tab path, not shrinking main. Next candidates if desired: PlannerWrapper (66 kB) is already lazy; the residual main chunk is mostly React + app shell — diminishing returns.
- **Tooling note**: `npm run build` is now `vite build && node scripts/stamp-sw.mjs`. If you add a build step, keep the stamper last — it rewrites `dist/sw.js` and throws if the placeholder is missing.

## Per-feature improvement notes

- **Style Me**: look quality per her four priority occasions; smarter sampling (the bucket targets are effectively uncapped — ~160 items is a typical surviving pool, not a cap; the pipeline predates hosiery/sets logic); watch how "Only" filters interact with sampling on small pools.
- **Style Me look editing (reworked 2026-08-13, owner request — "it feels like more code than it needs to be")**: the 2026-08-07 in-place editor (LookCard edit mode + SwapItemSheet) is REMOVED. Edit on a results card now opens the manual builder pre-filled (App `builderSeed`); saving lands in Looks, and the A1 edit-lesson signal is derived by diffing seed vs saved ids (slot-paired swaps) in the App builder-mount onSave. Still true: looks carry a stable `_uid` (normalizeLooks) for the list key; edits never re-run the validator (builder saves are her final word).
- **Closet**: bulk re-categorization UX, better dedup, surfacing RESTING/neglected pieces.
- **Crop / bg-removal / trim**: pipeline in `src/lib/bgRemoval.js`, `src/features/images/recutDrip.js`, `TrimmedImage.jsx`. Owner cares about isolation/trim quality; audit results on real items.
- **Saved/History**: both searchable now (History 2026-08-02, Saved Looks + Favorites this phase via `LookSearchContext` — cards filter themselves since the lists own their data). Maybe collage regeneration for old looks.
- **Shopping**: prompts are now brand-agnostic (taste inferred from the closet). Follow up on whether results feel smarter. (The Favorites > Shopping placeholder tab was removed 2026-08-08.)

## Working agreements

- Feature branch → PR → merge `main` (auto-deploy). Squash-merge with `(#NN)` in the title, matching history. **Parallel sessions ship to `main`**: always `git fetch origin main` and merge/rebase before pushing, and expect CHANGELOG conflicts at the top of the file.
- Update `CHANGELOG.md` per feature (house style: Why / Added / Changed / Fixed).
- Never break: `npm test` runs the whole battery — coerce (46), boxmath (11), validator (32), filters (38), freetext (7), rotation (22), weather (65), packer (64), edits (13), memory (32), silhouette (47), notes (21), taxonomy (4), matrix — plus `npm run build`. Run `npm ci` first in a fresh container.
- Multi-agent sessions: agents share the working tree — scope each agent to disjoint files, keep CHANGELOG/HANDOFF/commits with the orchestrator, commit per-feature with explicit file lists (never `git add -A` while agents run).
- Keep this HANDOFF.md current: when a session resolves or discovers items, rewrite the doc before ending — a stale handoff sends the next session chasing closed issues.
