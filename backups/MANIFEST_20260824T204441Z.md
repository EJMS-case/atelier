# Pre-migration backup — 2026-08-24T20:44:41Z

Taken before the multi-closet migration (closets table + `closet_id` on `wardrobe_items`).
No DDL had been run against the live database at the time of this backup.

## Files (this directory)

| File | Rows | Matches live count |
|---|---|---|
| `wardrobe_items_20260824T204441Z.json` | 480 | ✅ 480 |
| `sets_20260824T204441Z.json` | 41 | ✅ 41 |
| `outfit_logs_20260824T204441Z.json` | 101 | ✅ 101 |

All rows exported with all columns. One exception: `outfit_logs.collage_url` values that
are base64 `data:` URIs (~1.9 MB total) are replaced in the **file** export with a marker
noting their length; the full values are preserved verbatim in the DB snapshot below.
HTTP(S) collage URLs are kept as-is.

## DB-side snapshots (full fidelity, same moment)

Created in the same Supabase project (`ljcwsrfmojbjdveefoqa`), RLS enabled:

- `wardrobe_items_backup_20260824` — 480 rows
- `sets_backup_20260824` — 41 rows
- `outfit_logs_backup_20260824` — 101 rows (includes full base64 collages)

Restore path: `insert into wardrobe_items select * from wardrobe_items_backup_20260824`
(or per-row repair joins on `id`).
